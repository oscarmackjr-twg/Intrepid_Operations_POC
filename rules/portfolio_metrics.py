import pandas as pd

def compute_portfolio_metrics(df: pd.DataFrame, cfg: dict) -> pd.DataFrame:
    # TODO: implement notebook parity
    return pd.DataFrame(columns=["metric_id", "description", "value", "threshold", "pass_fail"])
