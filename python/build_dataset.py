# C:\...\loan_engine\python\build_dataset.py

def build_dataset(*args, **kwargs):
    raise RuntimeError(
        "This CLI script is no longer used by run_pipeline. "
        "Use pipeline.build_dataset.build_dataset instead."
    )
